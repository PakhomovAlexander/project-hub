# Case: check-with-no-command-fails-the-run

A malformed check line (no TAB, so no command) is not silently skipped: it fails the run
and is excluded from the executed total, so a typo cannot quietly shrink the gate.

Generated by generate.sh from the real harness — do not hand-edit.
The transcript is the fixture: every command, its stdout, its stderr, its exit code.
