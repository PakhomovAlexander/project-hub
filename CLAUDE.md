@AGENTS.md

That import is the whole point: this template repo keeps its agent instructions
single-sourced in [`AGENTS.md`](AGENTS.md) — the same pattern a generated hub uses
(rules live in `AGENTS.md`; `CLAUDE.md` imports them so Claude Code lands on the
same runbook as every other agent).

**If you were asked to "set up a project hub" (or "set up `<X>`"): follow
[`SETUP.md`](SETUP.md).**
